<%- include('./head') %>
<h1><%= title %></h1>
<ul>
  <% if(genre_list.length){ genre_list.forEach(function(genre){ %>
  <li>
    <a href=<%= genre.url%>><%= genre.name%> </a> 
  </li>
  <% })} else{ %>
  <p>There are no genres.</p>
  <% } %>
</ul>
<%- include('./foot') %>